# 更新日志

本文档记录此项目的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.1] - 2026-03-10

### 修复

- **Windows 窗口方向** — 根据任务栏位置智能判断展开方向（任务栏在底部时向上展开）
- **Windows 全屏查看空白** — 使用跨平台正确的 `file:///` URL 格式加载全屏 HTML 窗口
- **全屏后界面无响应** — 全屏窗口打开期间抑制主窗口自动隐藏，关闭后恢复
- **临时文件清理** — 全屏窗口关闭时自动清理临时 HTML 文件

### 变更

- **Chrome 扩展包命名** — 从 `clipstash-v*.zip` 改为 `clipstash-chrome-extension-v*.zip`，便于区分平台

## [0.1.0] - 2026-03-03

### 新增

- **剪切板缓存** — 支持文本、图片（PNG / JPEG Data URL）、HTML 富文本
- **内容去重** — 文本精确匹配，图片 SHA-256 哈希去重
- **系统托盘** — 左键点击切换弹出窗口
- **托盘右键菜单** — 缓存剪切板、打开、设置、开机自启、退出
- **弹出式窗口** — 无边框圆角窗口，失焦自动隐藏
- **自定义标题栏** — 可拖拽页头，显示应用图标与名称
- **全局快捷键** — `Alt+Shift+C` 一键缓存剪切板，可自定义
- **剪切板监听** — 可选 500ms 轮询自动缓存
- **搜索** — 实时搜索（250ms 防抖），按内容和标签过滤
- **标签** — 为每条缓存添加/移除标签，标签建议
- **置顶** — 重要缓存置顶显示
- **详情弹窗** — 点击卡片查看完整内容
- **全屏查看** — 在独立窗口中全屏展示
- **缓存上限** — 可配置 10 ~ 999 条，自动淘汰
- **导入导出** — JSON 格式，导入自动去重
- **主题** — 跟随系统 / 浅色 / 深色，完整 CSS 变量体系
- **国际化** — 中英双语（80+ 键值），即时切换
- **开机自启** — 可选开机启动
- **系统通知** — 可配置缓存事件通知
- **SQLite 存储** — 本地数据库，WAL 模式
- **跨平台** — macOS (.app / .dmg)、Windows (.exe)、Linux (.deb / .AppImage)
- **隐私** — 零网络请求，数据本地存储

[0.1.1]: https://github.com/lonsty/clipstash/releases/tag/v0.1.1
[0.1.0]: https://github.com/lonsty/clipstash/releases/tag/desktop/v0.1.0
